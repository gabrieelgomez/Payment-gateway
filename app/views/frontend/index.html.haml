
#front
	.front-container

		#home{style:"padding-top: 80px;"}
			= render 'home'
		
		#about{style:"padding-top: 80px;"}
			= render 'about'

		#video-full{style:"padding-top: 60px;"}
			= render 'video'

		#ventajas{style:"padding-top: 12px;"}
			= render 'ventajas'

		#conferences{style:"padding-top: 80px;"}
			= render 'conferences'

		#customers
			= render 'customers'
		#contacs
			= render 'contacts'
		#footer
			= render 'footer'

:javascript
	var elem = document.getElementById("video-henao");
	$( "#boton-play").click(function() {	
		if (elem.requestFullscreen) {
			elem.requestFullscreen();
		} else if (elem.msRequestFullscreen) {
			elem.msRequestFullscreen();
		} else if (elem.mozRequestFullScreen) {
			elem.mozRequestFullScreen();
		} else if (elem.webkitRequestFullscreen) {
			elem.webkitRequestFullscreen();
		}		
	});
