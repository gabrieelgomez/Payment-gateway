/.container-video
/	= link_to "#", class: "btn-play2", id:"boton-play2" do
/		%i.fa.fa-times#cerrar
/	%video#myvideo-bg{:controls => "",:autoplay => "", :loop => ""}
/		%source{:src => "//demosthenes.info/assets/videos/polina.webm", :type => "video/webm"}
/		%source{:src => "//demosthenes.info/assets/videos/polina.mp4", :type => "video/mp4"}
		
.front
	.front-container

		#home
			= render 'home'
		#about
			= render 'about'
		#video-full
			= render 'video'
		#ventajas
			= render 'ventajas'	

:javascript
	$( "#boton-play").click(function() {

		var elem = document.getElementById("video-henao");
		if (elem.requestFullscreen) {
		  elem.requestFullscreen();
		} else if (elem.msRequestFullscreen) {
		  elem.msRequestFullscreen();
		} else if (elem.mozRequestFullScreen) {
		  elem.mozRequestFullScreen();
		} else if (elem.webkitRequestFullscreen) {
		  elem.webkitRequestFullscreen();
		}
	});


/$('#myvideo-bg').css({"z-index":"100000"});
/$('#myvideo-bg').css({"opacity":"1"});
/$('#cerrar').css({"opacity":"0.5"});
/$('#myvideo-bg')[0].play();


/$( "#boton-play2" ).click(function() {
/	$('#myvideo-bg').css({"opacity":"0"});
/	$('#myvideo-bg').css({"z-index":"-100000"});
/	$('#cerrar').css({"opacity":"0"});
/	$('#myvideo-bg')[0].stop();
/});	



	